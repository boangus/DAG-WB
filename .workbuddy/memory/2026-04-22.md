

## DAG-WB v3.1 dagitty交互移植 (23:50)
- 修复App.jsx多处语法错误（缺少const声明、括号不匹配等）
- 实现dagitty风格交互：
  1. 点击画布空白区域 → 弹出输入框添加变量
  2. 点击节点A → 点击节点B → 创建有向边(A→B)
  3. 创建边时弹出选择：显示标签/隐藏标签
  4. 点击任意边 → 切换标签显示/隐藏
  5. 全局标签显示切换按钮（头部）
- 快捷键扩展：M=中介，I=工具，P=代理，X=修饰
- 双击节点快速重命名
- 拖拽边自动连接
- D键：如有连边则删除所有连边，否则删除节点
- **已推送GitHub并触发部署**（commit 7a912d9）

## DAG-WB v2.0 升级 (23:15)
- GitHub Pages部署成功，workflow已修复
- 实现连线自动判断边类型（inferEdgeType函数）基于Quimpo 2026方法学
- 连线规则：confounder→X/Y=混杂，exposure→outcome=直接效应，E→M→Y=中介，多父节点=碰撞
- dagre LR自动布局，ReactFlowProvider封装
- Toolbar和NodeEditor全面升级，角色颜色预览
